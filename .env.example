# === Runtime provider (default: local + free) ===
PROVIDER=ollama

# Ollama models (chat + embeddings)
OLLAMA_MODEL=llama3.1
EMBED_MODEL=nomic-embed-text
# Tip: if your machine is tight on RAM/VRAM, use:
# OLLAMA_MODEL=llama3.2:3b
# or
# OLLAMA_MODEL=phi3:mini

# System prompt (tweakable behavior)
SYSTEM_PROMPT=You are a concise, helpful assistant. If unsure, say so.

# Safety toggle (redact emails/phones before storing to memory)
SAFE_MODE=true

# --- Optional: OpenAI cloud (leave commented unless you switch providers) ---
# PROVIDER=openai
# OPENAI_API_KEY=sk-************************
# MODEL=gpt-4o-mini
